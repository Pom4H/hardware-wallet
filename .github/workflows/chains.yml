name: Chain integration

on:
  push:
    paths:
      - .github/workflows/chains.yml
      - Cargo.lock
      - crates/chain-api/**
      - crates/crypto-runtime/**
      - crates/key-lifecycle/**
      - crates/hd-key-backend/**
      - crates/chain-bitcoin/**
      - crates/chain-ethereum/**
      - crates/chain-solana/**
  pull_request:
    paths:
      - .github/workflows/chains.yml
      - Cargo.lock
      - crates/chain-api/**
      - crates/crypto-runtime/**
      - crates/key-lifecycle/**
      - crates/hd-key-backend/**
      - crates/chain-bitcoin/**
      - crates/chain-ethereum/**
      - crates/chain-solana/**
  workflow_dispatch:

permissions:
  contents: read

jobs:
  chain:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - chain: bitcoin
            package: hardware-wallet-chain-bitcoin
          - chain: ethereum
            package: hardware-wallet-chain-ethereum
          - chain: solana
            package: hardware-wallet-chain-solana

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Install Rust
        uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c

      # Exact commit: local nodes expose RPC/faucet capabilities only. Wallet
      # keys and signing material are derived entirely by this repository.
      - name: Start local chain
        uses: Pom4H/chain-sandbox@7bdf7388677b394e6cf94b7e65dfe3bffc2d778d
        with:
          chain: ${{ matrix.chain }}

      - name: Verify consumer RPC boundary
        shell: bash
        run: |
          case "${{ matrix.chain }}" in
            bitcoin)
              curl --fail --silent --show-error \
                --header 'content-type: text/plain;' \
                --data-binary '{"jsonrpc":"1.0","id":"wallet","method":"getblockcount","params":[]}' \
                "$BITCOIN_RPC_URL" | grep -q '"result":101'
              ;;
            ethereum)
              curl --fail --silent --show-error \
                --header 'content-type: application/json' \
                --data-binary '{"jsonrpc":"2.0","id":1,"method":"eth_chainId","params":[]}' \
                "$ETHEREUM_RPC_URL" | grep -qi '0x7a69'
              ;;
            solana)
              curl --fail --silent --show-error \
                --header 'content-type: application/json' \
                --data-binary '{"jsonrpc":"2.0","id":1,"method":"getHealth"}' \
                "$SOLANA_RPC_URL" | grep -q '"ok"'
              test -x "$SOLANA_CLI"
              ;;
          esac

      - name: Run chain crate tests
        run: cargo test --locked -p "${{ matrix.package }}"

      - name: Run Bitcoin lifecycle e2e
        if: matrix.chain == 'bitcoin'
        run: cargo run --locked -p hardware-wallet-chain-bitcoin --example bitcoin_e2e

      - name: Run Ethereum lifecycle e2e
        if: matrix.chain == 'ethereum'
        run: cargo run --locked -p hardware-wallet-chain-ethereum --example ethereum_e2e

      - name: Run Solana lifecycle e2e
        if: matrix.chain == 'solana'
        run: cargo run --locked -p hardware-wallet-chain-solana --example solana_e2e
